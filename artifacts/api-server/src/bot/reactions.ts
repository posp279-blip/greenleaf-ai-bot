// Fallback template reactions when AI is unavailable
// These are used as a backup when the Proxy API is down.
// Style: warm, friendly, conversational — like explaining to a friend.

export function getLaundryReaction(intent: string, brandName?: string): string {
  const brand = brandName || "это средство";
  switch (intent) {
    case "mass_market_brand":
      return `Понял, ${brand}. Его многие берут просто на автомате: бренд знакомый, запах привычный, часто бывает по акции.

Я не буду пугать тебя химией, тут не про страшилки.

Просто есть момент: после стирки одежда, полотенца и постель каждый день касаются кожи. Поэтому важно смотреть не только на запах и цену пачки, а ещё на состав, выполаскивание и расход.

Хочешь, я коротко покажу, почему такие привычные средства не всегда самый выгодный и спокойный вариант?`;

    case "eco_brand":
      return `Круто, значит ты уже смотришь, чем стираешь. Это редко.

Тогда вопрос будет не "хорошо ли оно", а "сколько оно стоит за год" и "есть ли альтернатива такого же уровня, но дешевле".

Давай просто сравним цифры.`;
    case "unknown":
      return `Нормально, многие не помнят название. Берут что под рукой или что по акции.

Давай возьмём средний пример популярного средства из магазина — логика будет понятна в любом случае.`;
    case "not_used":
      return `Окей, ты сам не стираешь. Но эта категория есть почти в каждом доме — если рядом есть кто-то, кто стирает, цифры всё равно будут полезны.`;
    default:
      return `Понял. Давай просто посмотрим, на что стоит обращать внимание в средствах для стирки.`;
  }
}

export function getDishReaction(intent: string, brandName?: string): string {
  const brand = brandName || "это средство";
  switch (intent) {
    case "mass_market_brand":
      return `Понял, ${brand}. Его тоже часто берут по привычке: пенится хорошо, бренд знакомый, цена вроде нормальная.

Только средство для посуды — это не просто бутылка у раковины. Оно каждый день контактирует с тарелками, чашками, ложками и руками.

Тут тоже лучше смотреть не только на цену бутылки, а на расход и состав.

Хочешь, я покажу простую логику, почему иногда выгоднее считать не цену бутылки, а стоимость месяца?`;
    case "eco_brand":
      return `Хороший выбор. Значит, ты уже обращаешь внимание на то, чем моется посуда.

Тогда смотрим не только состав, но и расход: сколько уходит за год и можно ли купить такое же качество выгоднее.`;
    case "unknown":
      return `Нормально. Тогда разберём на примере стандартного средства из магазина — всё равно логика будет понятна.`;
    case "not_used":
      return `Окей. Но эта категория есть почти в каждом доме — посмотрим вместе.`;
    default:
      return `Понял. Давай просто посмотрим, на что стоит обращать внимание в средствах для посуды.`;
  }
}

export function getPadsReaction(intent: string): string {
  switch (intent) {
    case "affirmative":
      return `Нормально, раньше не задумывался. Большинство выбирает по привычке, бренду или акции.

Смотри, тут логика простая: продукт контактирует с чувствительной зоной несколько часов подряд. Поэтому состав и материалы имеют значение.

Это не медицинская консультация — просто факт, который стоит знать.`;
    case "negative":
      return `Понятно. Но давай всё равно посмотрим — эта информация может быть полезна для близких.`;
    default:
      return `Понял. Давай по-человечески разберём, из чего обычно делают прокладки и почему на эту категорию стоит посмотреть внимательнее.`;
  }
}

export function getToiletReaction(intent: string, brandName?: string): string {
  switch (intent) {
    case "unknown":
      return `Понятно, мало кто запоминает бренд туалетной бумаги. Берут что под рукой.

Давай разберём на типичных примерах.`;
    case "not_used":
      return `Окей. Но эта категория есть в каждом доме — посмотрим вместе.`;
    default:
      return `Понял${brandName ? `, это "${brandName}"` : ""}. Здесь даже не так важен бренд. Важно другое: туалетная бумага — это регулярный контакт с чувствительными зонами. Почти никто не смотрит на состав и расход. Давай посмотрим вместе.`;
  }
}

export function getObjectionPriceResponse(): string {
  return `Понимаю, цена — это то, что видно сразу.

Но смотри: вопрос не "сколько стоит одна упаковка", а "сколько уходит за год на все привычные товары".

Если коротко — за счёт концентрата и меньшего расхода итоговая годовая сумма часто оказывается ниже. Давай просто сравним цифры.`;
}

export function getObjectionPyramidResponse(): string {
  return `Понимаю вопрос. Многие так думают, это нормально.

Давай по-честному: Greenleaf платит за товарооборот, а не за пустую регистрацию. Когда люди реально покупают и пользуются продукцией — создаётся товарооборот, с которого начисляются бонусы.

Это другая логика. Продолжим разбор — в конце станет понятнее.`;
}

export function getEarlyRegistrationResponse(): string {
  return `Понял. Но давай сначала я покажу тебе разбор и цифры, чтобы решение было осознанным. Это займёт несколько минут.

Потом, если будет интересно, я сам предложу открыть условия Greenleaf.`;
}
